{
  "name": "ecommerce-prototype",
  "version": "1.0.0",
  "description": "E-commerce prototype with React frontend and Express backend",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ..",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "echo 'Backend doesn't need building' || true"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
